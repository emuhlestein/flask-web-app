# Flask Docker Compose Project
 
Must have docker desktop for windows installed and running.

This is a simple Flask application containerized with Docker Compose.
 
## How to Run
 
1. Build and start the containers:
   ```bash

   docker-compose up --build

Open your browser and navigate to http://localhost:5000